# Schema for GitHub Actions Secrets / Variables (not used as a file on the VPS).
PORT=8787
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
DATA_DIR=/var/lib/bacpq
POLL_MS=2000
